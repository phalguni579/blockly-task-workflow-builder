# 🧩 Visual Task Workflow Builder (Blockly)

An interactive web application built using **Blockly + Vanilla JavaScript** that allows users to visually design task workflows, generate JavaScript code, and execute it with live results.

This project demonstrates **custom Blockly blocks**, **JavaScript code generation**, and **runtime execution** — all without using any frontend framework.

---

## 🚀 Features

- 🧱 Custom Blockly blocks (Create Task, Set Deadline, Log Task, Calculate ETA, End Workflow)
- 🔄 Drag-and-drop workflow creation
- 🧠 Automatic JavaScript code generation
- ▶️ Execute generated JavaScript safely
- 📟 Live output display
- 🎨 Clean and responsive UI
- ⚙️ Built using Vanilla JavaScript (no React / Vue / Angular)

---

## 🛠️ Tech Stack

- **JavaScript (ES6+)**
- **Blockly (latest version)**
- **HTML5**
- **CSS3**

---

## 🧩 Custom Blocks Implemented

1. **Create Task** – Define task name and priority  
2. **Set Deadline** – Assign deadline in days  
3. **Log Task Details** – Display task information  
4. **Calculate ETA** – Estimate completion time based on priority  
5. **End Workflow** – Mark workflow completion  

---

## ▶️ How to Run Locally

1. Clone the repository:
   ```bash
   git clone https://github.com/<your-username>/blockly-task-workflow-builder.git
